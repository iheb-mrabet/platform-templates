name: CI

on:
  push:
    branches:
      - main
  pull_request:

jobs:
  phase3-placeholder:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Phase 3 placeholder
        run: |
          echo "This service was generated by Backstage."
          echo "Full security CI will be implemented in Phase 4."